// JavaScript is a dynamic weakly typed programming language which
// is compiled at runtime. It can be executed as part of a webpage in
// a browser or directly on a machine("Host machine").return.apply
// It was created to make the contents of the webpages dynamic.

//User ==>url ==>client==>html request to the server <== response from the server.
//javascript makes the process reactive.
//JS is : dynamic -weakly typed, interpreted, runs on browser, hosted language.
//How is JS executed??::using the JS engine which is built into the browser
// to build tht JS, compile it , parse the js code: eg V8(Chrome) , Spider Monkey(Firefox)
